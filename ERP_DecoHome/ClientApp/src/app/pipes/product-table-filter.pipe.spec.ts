import { ProductTableFilterPipe } from './product-table-filter.pipe';

describe('ProductTableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductTableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
